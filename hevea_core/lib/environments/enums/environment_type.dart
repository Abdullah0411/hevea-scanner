enum EnvironmentType {
  /// Environment where the developers play around and have fun ;)
  development,
  /// A copy from the production environment to test the environment before deployment to production
  staging,
  /// Live production environment
  production,
}